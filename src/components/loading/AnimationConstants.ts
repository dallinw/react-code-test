/**
 * Values for loading screen and transition animations, collected in one place
 */

const AnimationConstants = {
  offset: 800,
  period: 2500,
  loadingMS: 3000,
  fadeOutMS: 1500,
};

export default AnimationConstants;
